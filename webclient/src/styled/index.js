import styled, { keyframes, css } from 'styled-components';

// potentially: global styles obj lives here

export const ScreenContainer = styled.div`
    display: flex;
    flex-direction: column;
    width: 100vw;
    padding: 1rem;
`;